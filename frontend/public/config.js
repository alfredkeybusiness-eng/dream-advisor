// Edit after the backend's Render URL is known (or once a real build step
// exists to inject it). The placeholder index.html reads this global.
window.DREAM_ADVISOR_API = window.DREAM_ADVISOR_API || "http://localhost:8000";
